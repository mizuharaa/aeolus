# =============================================================================
# Olus — Airline Disruption Simulation & Recovery Engine
# Docker Compose — Full Stack
#
# Olus is a deterministic in-memory simulator: the API loads the Nimbus Air
# network from YAML on startup and holds all state in process. There is no
# database, cache, queue, or auth tier to provision — just the API and the web
# frontend. (Live data is pulled over HTTPS from OpenSky / NWS / FAA at runtime.)
# =============================================================================

networks:
  olus-net:
    driver: bridge

services:

  # ---------------------------------------------------------------------------
  # Olus API — FastAPI / Python backend
  # ---------------------------------------------------------------------------
  olus-api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
      target: production
    container_name: olus-api
    restart: unless-stopped
    environment:
      # App
      APP_ENV: ${APP_ENV:-development}
      DEBUG: ${DEBUG:-false}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:3000}
      # Optimizer
      SOLVER_TIMEOUT_SECS: ${SOLVER_TIMEOUT_SECS:-30}
      USE_HEURISTIC_FALLBACK: ${USE_HEURISTIC_FALLBACK:-true}
      # Weather
      WEATHER_FETCH_INTERVAL_SECS: ${WEATHER_FETCH_INTERVAL_SECS:-300}
      # OpenSky Network (optional — anonymous if blank)
      OPENSKY_CLIENT_ID: ${OPENSKY_CLIENT_ID:-}
      OPENSKY_CLIENT_SECRET: ${OPENSKY_CLIENT_SECRET:-}
      OPENSKY_USERNAME: ${OPENSKY_USERNAME:-}
      OPENSKY_PASSWORD: ${OPENSKY_PASSWORD:-}
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 45s
    networks:
      - olus-net
    volumes:
      # Hot-reload: mount source + data for local dev (comment out for prod)
      - ./apps/api/src:/app/apps/api/src:ro
      - ./data:/app/data:ro
      - ./credentials:/app/credentials:ro
    labels:
      com.olus.service: "api"
      com.olus.version: "0.1.0"

  # ---------------------------------------------------------------------------
  # Olus Web — Next.js 14 frontend
  # ---------------------------------------------------------------------------
  olus-web:
    build:
      context: apps/web
      dockerfile: Dockerfile
      target: production
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8000}
        NEXT_PUBLIC_WS_URL: ${NEXT_PUBLIC_WS_URL:-ws://localhost:8000}
    container_name: olus-web
    restart: unless-stopped
    environment:
      NODE_ENV: production
      # Server-side REST proxy target (container network, not browser-visible).
      API_URL: http://olus-api:8000
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8000}
      NEXT_PUBLIC_WS_URL: ${NEXT_PUBLIC_WS_URL:-ws://localhost:8000}
    ports:
      - "3000:3000"
    depends_on:
      olus-api:
        condition: service_healthy
    healthcheck:
      # Alpine image has wget but no curl, and Next.js (standalone) binds IPv4
      # only — `localhost` resolves to IPv6 ::1 in some Alpine images and fails.
      test: ["CMD-SHELL", "wget -q -O - http://127.0.0.1:3000/ > /dev/null || exit 1"]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 60s
    networks:
      - olus-net
    labels:
      com.olus.service: "web"
      com.olus.version: "0.1.0"
